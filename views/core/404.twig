<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Route not found</title>
    <style>
        {% include '@editor/assets/styles/base/variables.css' %}
        {% include '@editor/assets/styles/base/reset.css' %}

        .main {
            position: relative;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            height: 100vh;
            width: 100vw;
        }

        .card {
            position: relative;
            width: 75%;
            max-width: 464px;
            padding: 32px;
            background: var(--card);
            border-radius: var(--radius);
            box-shadow: var(--shadow-sm);
            border: 1px solid var(--border);
        }

        h1 {
            margin: 0;
            font-size: 1.5rem;
            line-height: 1.2;
            color: var(--foreground);
            font-weight: 600;
            letter-spacing: -0.025em;
        }

        h1 + p {
            margin-top: 12px;
            color: var(--muted-foreground);
            font-size: 1rem;
            line-height: 1.6;
        }

        p {
            margin: 0;
            padding: 0;
        }

        p + p {
            margin-top: 16px;
        }

        p:last-of-type {
            margin-bottom: 0;
        }

        hr {
            border: 0;
            height: 1px;
            background: var(--border);
            margin: 24px 0;
        }

        .help-text {
            font-size: 0.875rem;
            color: var(--muted-foreground);
            line-height: 1.6;
        }

        a {
            margin: 0;
            font-weight: 600;
            color: var(--primary);
            text-decoration-skip-ink: all;
            text-decoration-thickness: 1px;
            text-underline-offset: 2px;
            text-decoration-color: rgb(var(--primary) / 0.5);
            transition: text-decoration-color 0.15s ease-in-out;
        }

        a:hover,
        a:focus-visible {
            text-decoration-color: var(--primary);
        }

        .inline-code {
            display: inline-block;
            background: var(--secondary);
            color: var(--secondary-foreground);
            padding: 6px 10px;
            border-radius: var(--radius-sm);
            font-size: 0.875rem;
            overflow-wrap: anywhere;
            font-family: var(--font-mono);
            user-select: all;
            margin-top: 8px;
            width: 100%;
            box-sizing: border-box;
        }

        .steps {
            margin-top: 16px;
            list-style: none;
            padding: 0;
        }

        .steps li {
            margin-top: 8px;
            padding-left: 20px;
            position: relative;
        }

        .steps li:before {
            content: "•";
            position: absolute;
            left: 0;
            color: var(--primary);
        }

        @media (max-width: 640px) {
            .card {
                width: 90%;
                padding: 24px;
            }
        }
    </style>
</head>
<body>
    <div class="main">
        <div class="card">
            <h1>Route not found</h1>
            
            {% if function('current_user_can', 'manage_options') %}
                <p>
                    This route has not been configured. To handle this URL, you'll need to create a Layout that matches this route.
                </p>
                <hr>
                <p class="help-text">
                    Current route:
                    <code class="inline-code">{{ function('get_current_url') }}</code>
                </p>
                <p class="help-text">
                    To handle this route:
                </p>
                <ul class="steps help-text">
                    <li>Go to <a href="{{ function('admin_url', 'post-new.php?post_type=layout') }}">Layouts → Add New</a></li>
                    <li>Set Location Rules to "Template"</li>
                    <li>Set Context Type to "404"</li>
                    <li>Add your desired components</li>
                    <li>Publish the layout</li>
                </ul>
            {% else %}
                <p>
                    The requested URL could not be found. You may have followed a broken link or entered a URL that doesn't exist.
                </p>
                <hr>
                <p class="help-text">
                    Please check the URL and try again, or contact the site administrator if you believe this is an error.
                </p>
            {% endif %}
        </div>
    </div>
</body>
</html>
